spring:
  datasource:
    url: jdbc:sqlserver://localhost:1433;database=liquibase-test;
    username: liquibase
    password: liquibase
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.SQLServer2012Dialect
  liquibase:
    enabled: false